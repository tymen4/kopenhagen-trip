# Copenhagen, together

A tiny web app to plan a Copenhagen trip one morning at a time. Answer five mood questions, get five matching activities with a map. Runs entirely in the browser.

## Run locally
```
python -m http.server 8000
```
Then open `http://localhost:8000` on your phone on the same wifi (use your PC's IP instead of localhost).

## Deploy
Hosted on GitHub Pages.
